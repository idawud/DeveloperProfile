name: Java CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Setup SSH-Agent
      uses: webfactory/ssh-agent@v0.2.0
      with:
        ssh-private-key: |
          ${{ secrets.EC2 }} 
